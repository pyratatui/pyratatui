name: Cloud MkDocs Deploy

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repo
        uses: actions/checkout@v3

      # 2️⃣ Setup Python
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      # 3️⃣ Install MkDocs, Material, mkdocstrings, and extensions
      - name: Install MkDocs and dependencies
        run: |
          pip install --upgrade pip
          pip install mkdocs mkdocs-material "mkdocstrings[python]" pymdown-extensions tachyonfx

      # 4️⃣ Build MkDocs site
      - name: Build MkDocs site
        run: mkdocs build --clean

      # 5️⃣ Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GH_PAGES_TOKEN }}  # Personal Access Token with repo write access
          publish_dir: ./site
          publish_branch: gh-pages
          force_orphan: true
          
